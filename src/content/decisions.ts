/**
 * Жоба авторының нақты шешімдері — Word құжатында жоқ, бірақ автор өзі берген ережелер.
 * Word мазмұнынан (lesson.ts) бөлек сақталады, әр шешімнің күні мен себебі көрсетіледі.
 */

/**
 * 2-экран «Саяхатшының сөмкесі»: саяхатқа қажет емес заттар (қызылмен «қате» болып белгіленеді).
 * Шешім: 2026-09-24, автор — «доп пен балмұздақ қызыл болып қате болуы керек».
 * Балл бұрынғыдай мұғалім арқылы қойылады.
 */
export const BAG_NOT_NEEDED = ['доп', 'балмұздақ'] as const;

/**
 * 9-экран «Артық сөзді тап»: әр жолдың артық сөзі (Word-та жазылмаған, автор 2026-09-25
 * «дұрыс жауап таңдалса балл жинайтындай ет» деген соң қолданылды).
 */
export const ODD_WORD_ANSWERS: readonly string[] | null = ['мектеп', 'дәптер', 'жазады'];

/**
 * Автоматты балл ережелері (автор, 2026-09-25): дұрыс жауап берілгенде балл өздігінен қосылады,
 * мұғалім Teacher Mode-та түзете алады.
 *  2 — қажетті зат сөмкеде → 1; сөйлем құралды → 1
 *  5 — үш сурет дұрыс сәйкестендірілді → 1 (ауызша толық жауап — мұғалім)
 *  8 — суреттен 2 нысан табылды → 1; сөйлем құралды → 1
 *  9 — үш жолда да артық сөз дұрыс → 1
 * 12 — «Тексеру»: 5/5 → 2, 3–4 → 1, одан аз → 0
 * 14 — бір билеттің сұрағына жауап берілді → 1
 */
export function trueFalsePoints(correct: number): number {
  if (correct >= 5) return 2;
  if (correct >= 3) return 1;
  return 0;
}
